<% @items.each do |item| %>
  <%= link_to customer_item_path(item.id) ,style: "color: black;", data: {"turbolinks" => false} do %>
    <%= attachment_image_tag item, :image, format: 'jpeg', size: "40x40" %><br>
    <%= item.name %><br>
    ¥<%= item.add_tax_price.to_s(:delimited, delimiter: ',') %>
  <% end %>
<% end %>