<h2>マイページ</h2>
  <thead>
    <tr>
    <th>登録情報</th>
   <th><%= link_to "編集する",edit_customer_path(@customer)%></th>
    <!--上記、編集ページ-->
    </tr>
  </thead>
 <table>
   <thead>
    <td>
    <tr>
     <p>氏名:<%= @customer.full_name %></p>
     <p>カナ：<%= @customer.full_name_kana %></p>
     <p>郵便番号：<%= @customer.postal_code %></p>
     <p>住所：<%= @customer.address %></p>
     <p>電話番号：<%= @customer.phone_number %></p>
     <p>メールアドレス：<%= @customer.email %></p>
    </td>
    </thead>
    <p>配送先 <%= link_to "一覧を見る", addresses_path %></p>
    <p>注文履歴 <%= link_to "一覧を見る", orders_path %></p>
    </tr>

    </table>